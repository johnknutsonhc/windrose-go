version: v1.1.0
steps:
  - build: -t $Registry/windrose-go:$ID -f Dockerfile .
  - cmd: docker.io/checkmarx/kics:latest scan
  - push:
    - $Registry/windrose-go:$ID
    - $Registry/windrose-go:latest
